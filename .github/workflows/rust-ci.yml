name: Rust CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  ci:
    name: CI (Format, Clippy, Test)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Check formatting
        run: cargo +nightly fmt --all --check

      - name: Run clippy
        run: cargo +nightly clippy --workspace --all-targets --all-features -- -D warnings

      - name: Run tests
        run: cargo +nightly test --workspace --all-features --lib --bins --tests

  # build:
  #   name: Build Check
  #   runs-on: ubuntu-latest
  #   needs: [format, clippy, test]
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - uses: dtolnay/rust-toolchain@master
  #       with:
  #         toolchain: 1.84.1
  #
  #     - uses: Swatinem/rust-cache@v2
  #       with:
  #         cache-on-failure: true
  #
  #     - name: Build release
  #       run: cargo build --workspace --release
